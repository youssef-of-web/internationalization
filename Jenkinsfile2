pipeline {
    agent any
    stages {
        stage ("install") {
           steps {
             nodejs('NodeJs_Builder') {
                sh "npm i"
              }
           }
        }
        stage ("build") {
           steps {
             nodejs('NodeJs_Builder') {
                sh "npm run build"
              }
           }
        }
    }
}